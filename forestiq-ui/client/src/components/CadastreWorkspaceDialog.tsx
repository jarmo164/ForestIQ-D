import { Building2, CalendarClock, ClipboardList, FileText, X } from "lucide-react";

type Item = Record<string, unknown>;
export type CadastreWorkspace = { cadastre: Item; owners: Item[]; activities: Item[]; notifications: Item[]; registryFeatures: Item[]; customerSummary: Item };

function text(value: unknown) { return value === null || value === undefined || value === "" ? "—" : String(value); }
function date(value: unknown) { return typeof value === "number" ? new Date(value).toLocaleString("et-EE") : text(value); }

export function CadastreWorkspaceDialog({ cadastreId, data, loading, error, onClose }: { cadastreId: string | null; data: CadastreWorkspace | null; loading: boolean; error: string | null; onClose: () => void }) {
  if (!cadastreId) return null;
  const cadastre = data?.cadastre ?? {};
  return <div className="fixed inset-0 z-50 flex items-end bg-[#10281f]/45 p-0 backdrop-blur-sm md:items-center md:justify-center md:p-6" role="dialog" aria-modal="true" aria-label="Katastriüksuse detailvaade"><section className="max-h-[94vh] w-full max-w-5xl overflow-hidden rounded-t-[2rem] bg-[#fbfdf9] shadow-2xl md:rounded-[2rem]"><header className="flex items-start justify-between border-b border-[#dce8d9] bg-white px-6 py-5"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#387155]">Kaardikeskne tööruum</p><h2 className="mt-1 font-serif text-2xl font-semibold text-[#17342a]">{text(cadastre.name) === "—" ? `Katastriüksus ${cadastreId}` : text(cadastre.name)}</h2><p className="mt-1 font-mono text-sm text-[#60756a]">{cadastreId}</p></div><button onClick={onClose} className="rounded-full p-2 text-[#466356] hover:bg-[#edf5ed]" aria-label="Sulge detailaken"><X className="h-5 w-5" /></button></header>{loading ? <div className="p-10 text-center text-[#60756a]">Katastri tervikandmeid laaditakse…</div> : error ? <div className="p-10 text-center text-[#a52945]">{error}</div> : data ? <div className="max-h-[calc(94vh-110px)] overflow-y-auto p-5 md:p-7"><div className="grid gap-4 md:grid-cols-4"><Card label="Aadress" value={cadastre.address} /><Card label="Pindala" value={cadastre.area ? `${text(cadastre.area)} ha` : null} /><Card label="Maakond" value={cadastre.county} /><Card label="Kliendisuhtega omanikud" value={data.customerSummary.customerOwnerCount} /></div><div className="mt-7 grid gap-6 lg:grid-cols-2"><Panel icon={<Building2 className="h-4 w-4" />} title="Omanikud ja kliendisuhe"><div className="space-y-3">{data.owners.map(owner => { const relationship = (owner.customerRelationship ?? {}) as Item; return <div key={text(owner.id)} className="rounded-xl border border-[#dbe8d8] bg-white p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold">{text(owner.name)}</p><p className="text-sm text-[#65786b]">{text(owner.phone)} · {text(owner.email)}</p></div><span className="rounded-full bg-[#edf5ed] px-2 py-1 text-xs font-bold text-[#236047]">{text(relationship.isCustomer) === "true" ? "Klient" : text(relationship.ownerStatus)}</span></div><div className="mt-3 grid grid-cols-2 gap-2 text-xs text-[#60756a]"><span>Aktiivseid tehinguid: <b>{text(relationship.activeDealCount)}</b></span><span>Võidetud tehinguid: <b>{text(relationship.wonDealCount)}</b></span></div></div>})}</div></Panel><Panel icon={<CalendarClock className="h-4 w-4" />} title="Tegevuste ajalugu"><div className="space-y-3">{data.activities.length ? data.activities.map(activity => <div key={`${text(activity.kind)}-${text(activity.id)}`} className="border-l-2 border-[#8bbfa1] pl-3"><p className="text-sm font-medium">{text(activity.text ?? activity.message)}</p><p className="mt-1 text-xs text-[#6b7e72]">{text(activity.ownerName)} · {date(activity.at ?? activity.createdAt)}</p></div>) : <Empty text="Tegevusajalugu puudub." />}</div></Panel><Panel icon={<FileText className="h-4 w-4" />} title="Teatised"><div className="space-y-3">{data.notifications.length ? data.notifications.map(notification => <div key={text(notification.notificationId)} className="rounded-xl border border-[#f1d4da] bg-[#fff9fa] p-3 text-sm"><b>Teatis {text(notification.notificationNumber)}</b><p className="mt-1 text-[#6b5258]">Eraldis {text(notification.cadastreSubPartCode)} · {text(notification.workCode)} · staatus {text(notification.state)}</p><p className="mt-1 text-xs text-[#80636b]">Registreeritud: {date(notification.registrationDate)}</p></div>) : <Empty text="Aktiivseid teatisi ei ole." />}</div></Panel><Panel icon={<ClipboardList className="h-4 w-4" />} title="Metsaregistri andmed"><div className="space-y-3">{data.registryFeatures.length ? data.registryFeatures.map(feature => <div key={text(feature.id)} className="rounded-xl border border-[#e1dcf0] bg-[#fbfaff] p-3 text-sm"><b>{text(feature.title)}</b><p className="mt-1 text-[#5f5974]">Eraldis {text(feature.subpartCode)} · {text(feature.workCode)} · {text(feature.area)} ha</p><p className="mt-1 text-xs text-[#79718f]">Otsus: {text(feature.decision)} · Maht: {text(feature.volume)}</p></div>) : <Empty text="Metsaregistri objekte ei leitud." />}</div></Panel></div></div> : null}</section></div>;
}

function Card({ label, value }: { label: string; value: unknown }) { return <div className="rounded-xl border border-[#dbe8d8] bg-white p-4"><p className="text-xs font-bold uppercase tracking-wider text-[#6d8174]">{label}</p><p className="mt-1 text-sm font-semibold text-[#17342a]">{text(value)}</p></div>; }
function Panel({ icon, title, children }: { icon: React.ReactNode; title: string; children: React.ReactNode }) { return <section className="rounded-2xl border border-[#dce8d9] bg-[#fdfefd] p-5"><h3 className="mb-4 flex items-center gap-2 text-sm font-bold text-[#28624d]">{icon}{title}</h3>{children}</section>; }
function Empty({ text }: { text: string }) { return <p className="text-sm text-[#6b7e72]">{text}</p>; }
